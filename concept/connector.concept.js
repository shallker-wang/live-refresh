/* concept

var Connector = require('./connector')
var connector = new Connector(socket)

.on('connected', function(connection) {
  .sendMessage()
  .sendRefresh()
  .close()
  .on('close', function() {})
  .on('error', function() {})
  .off()  
})
.off()

*/