#!/usr/bin/env node

require("coffee-script")
var commander = require('commander');
var liverefresh = require('../lib/liverefresh');

commander
  .version(liverefresh.version)
  .parse(process.argv);

if (commander.args.length) {
  return liverefresh.refresh(commander.args.shift());
}

commander.help();
